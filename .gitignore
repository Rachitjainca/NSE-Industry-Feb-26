# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
env/

# Generated data & cache files
*.json.cache
# NOTE: *_cache.json files are NOW TRACKED — needed by GitHub Actions
#       so it doesn't re-fetch from inception on every run
# NOTE: nse_fo_aggregated_data.csv is KEPT (main output)
# NOTE: nse_tbg_daily_consolidated.csv is KEPT (data source)
# NOTE: reg_investors_cache/ is KEPT (historical data)
reg_investors_cache_temp/

# Generated logs (runtime)
*.log
collector.log
scheduler.log

# Service account keys — stored as GitHub Secrets, never commit
*service-account*.json
groww-data*.json
*credentials*.json

# Temporary files
temp_nse_downloads/
temp_fetch_missing.py
temp_nse_cat_probe.py

# OS
.DS_Store
Thumbs.db
